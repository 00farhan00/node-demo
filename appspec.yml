version: 0.0
os: linux
files:
  - source: webapp.war
    destination: /opt/tomcat/webapps/
hooks:
  ApplicationStop:
    - location: /opt/tomcat/bin/shutdown.sh
  BeforeInstall:
    - location: 
      timeout: 300
      runas: root
  AfterInstall:
    - location: /opt/tomcat/bin/startup.sh
      timeout: 300
      runas: root

